import { FunctionComponent } from "react";
import { Row } from "react-bootstrap";

const Home: FunctionComponent = function () {
    return(
        <Row className="mt-5">
            Please select the figure you want to calculate the area from the navigation bar
        </Row>
    )
}

export default Home